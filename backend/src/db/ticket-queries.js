import pool from './connection.js';

export const insertar = async (datos) => {};

export const listarPorUsuario = async (usuarioId) => {};

export const encontrarPorId = async (id) => {};

export const encontrarPorUuid = async (uuid) => {};

export const actualizarEstado = async (id, estado) => {};

export const decrementarStock = async (categoriaTicketId) => {};
