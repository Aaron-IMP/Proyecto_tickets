import pool from './connection.js';

export const listarEventos = async () => {};

export const encontrarPorId = async (id) => {};

export const insertar = async (datos) => {};

export const actualizar = async (id, datos) => {};

export const eliminar = async (id) => {};
